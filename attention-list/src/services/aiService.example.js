const DEEPSEEK_API_KEY = 'your-api-key-here';
const DEEPSEEK_API_URL = 'https://api.deepseek.com/v1/chat/completions';

export const analyzeAttention = async (records, triangleData) => {
  // 实现与 aiService.js 相同的功能，但不包含实际的 API 密钥
  // 这个文件会被提交到 GitHub，作为其他开发者的参考
};

export const sendChatMessage = async (message, triangleData, chatHistory, records) => {
  // 实现与 chatService.js 相同的功能，但不包含实际的 API 密钥
  // 这个文件会被提交到 GitHub，作为其他开发者的参考
}; 